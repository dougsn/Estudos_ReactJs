
import Routes from "./router";

function App() {
  return (
      <Routes />
  );
}

export default App;

// @@ Só foi importado o <Routes>, pois no arquivo de configuração das rotas, já foi agrupado as demais páginas e foi exportado o apenas 1 componente que foi importado nessa página. 